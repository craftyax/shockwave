#!/usr/bin/env python

from setuptools import setup

setup(name='shockwave',
      version='0.0.0',
      author='Erik L. Axdahl',
      packages=['shockwave'],
      install_requires=['numpy', 'Cantera'])
